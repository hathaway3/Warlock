<nav class="navbar">
    <div class="navbar-content">
        <a href="/" class="logo">
            <img src="<%= assetUrl('/assets/media/logos/warlock/warlock-lopoly-logo-64x48.webp') %>" alt="Warlock Logo" class="logo-img">
            <span>
                Warlock
                <span class="sub-title"><br/>Game Server Manager</span>
            </span>
        </a>
        <ul class="nav-links">
            <li><a href="/dashboard" class="nav-link" data-page="dashboard">
                <i class="fas fa-chart-line"></i> Dashboard
            </a></li>
            <li><a href="/hosts" class="nav-link" data-page="hosts">
                <i class="fas fa-desktop"></i> Hosts
            </a></li>
            <li><a href="/settings" class="nav-link" data-page="settings">
                <i class="fas fa-cog"></i> Settings
            </a></li>
            <li>
                <a href="https://github.com/hathaway3/Warlock" class="nav-link" target="_blank" rel="noopener">
                    <i class="fab fa-github"></i> GitHub
                </a>
            </li>
        </ul>
    </div>
</nav>

<script>
    // Highlight active page based on current URL
    (function() {
        const currentPath = window.location.pathname;
        const navLinks = document.querySelectorAll('.nav-link');
        
        navLinks.forEach(link => {
            link.classList.remove('active');
            const href = link.getAttribute('href');
            
            if (href === currentPath || 
                (currentPath === '/' && link.dataset.page === 'dashboard') ||
                (currentPath === '/index' && link.dataset.page === 'dashboard') ||
                (currentPath.includes(link.dataset.page))) {
                link.classList.add('active');
            }
        });
    })();
</script>
